# DEMO18-00 baseline and route authority record

Status: `COMPLETE — pre-change evidence recorded; DEMO18-01/02 implementation in progress`

Date: 2026-08-03

## Baseline retained

- Branch: `codex/v2-financial-close-core`
- Observed commit: `c41255635bee7e594e4b867cf26f5691b98097cc`
- Worktree: dirty before work. No reset, clean, checkout, or deletion was performed. The pre-existing
  dirty scope includes V2 backend/remediation, FigmaMake candidate, fixtures, documents and evidence.
- Runtime: `bravo-v2-api-1`, `bravo-v2-postgres-1` and `bravo-v2-redis-1` were running locally.
  The supported host ports observed were API `127.0.0.1:8000` and Postgres `127.0.0.1:55432`.
- Applied runtime migration: `0020_case_v2_audit (head)`, read from the API container using
  `alembic current`.

## Pre-change candidate gates

All commands ran from `FigmaMake_UI/` with the checked-in dependency tree:

| Gate | Result |
|---|---|
| `pnpm.cmd run typecheck` | pass |
| `pnpm.cmd run test` | 17 tests, pass |
| `pnpm.cmd run test:contract` | 7 tests, pass |
| `pnpm.cmd run test:a11y` | 1 test, pass |
| `pnpm.cmd run test:visual` | 2 tests, pass |
| `pnpm.cmd run build` | pass |

The same typecheck/test/build sequence was re-run after the first route and conversation-client
implementation and passed. The jsdom canvas notice from axe remains a test-runtime notice, not an
axe failure.

## Frozen normal-route matrix

| Route | Provider after containment | Status |
|---|---|---|
| `/`, `/c/:conversationId`, `/conversations` | authenticated Conversation CRUD and POST-SSE client | live contract integration |
| `/shared/:token` | unauthenticated, read-only shared conversation API | live contract integration |
| `/work`, `/work/new/:caseType`, `/work/cases/:caseId` | existing typed AccountingCase API client | live/partial; two-identity runtime exit remains open |
| `/financial-close`, `/accounting-work` | truthful redirect to an enabled Accounting Work route | alias only |
| all other normal paths | redirect to `/`; no prototype page is rendered | contained |
| `/review?qa=1` | QA-only fixture provider | explicit QA path; never normal navigation |

Normal navigation exposes only Knowledge Chat, conversation history and Accounting Work when the
returned capability permits it. It does not prefetch or label Money Engine, Anomaly, Tax, Graph,
Approvals, prototype Admin, Financial Close graph, or QA index.

## Must-not claims

- A Chat response is not an accounting conclusion, executed ERP action, or verified source unless
  the server returns matching evidence.
- The browser does not calculate reconciliation, voucher or close-readiness facts.
- A case export is an `artifact_produced_not_executed` result, not a post, payment, close or lock.
- Fixture IDs and QA results are never sent through the live conversation client.
- The local synthetic demo does not prove SME quality, A/B/C/D superiority, pilot eligibility,
  customer-data authorization, recovery readiness, or production readiness.

## Plan 17 status reconciliation

Plan 17's summary table says FEV2-02 through FEV2-09 are complete while several historical detailed
checkboxes remain unchecked. The authoritative interpretation for Plan 18 is: implementation and
fixture/browser-scope evidence exist, but FEV2-10 stays blocked until a live maker/reviewer handoff,
bundle hash and rollback record pass. Plan 18 does not treat the earlier status wording as a live
API or final-shell exit.
