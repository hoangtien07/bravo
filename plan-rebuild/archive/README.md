# Rebuild research archive

Status: `ARCHIVE INDEX`

This directory preserves generated research inputs and superseded rebuild material. Its contents
are excluded from default AI context. Current rebuild decisions are listed in `../README.md` and
the repository state is owned by `../../docs/PROJECT-STATE.md`.
