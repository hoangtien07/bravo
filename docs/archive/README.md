# Documentation archive

Status: `ARCHIVE INDEX`

This directory preserves superseded project documentation for provenance. Its contents are not
current project state and must not be loaded into default AI review context. Open an archived file
only to verify a specifically named historical claim.

Current project status is owned by `../PROJECT-STATE.md`.
